spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/services  #location for configs to other services
  profiles:
    active: native
  security:
    user:
      password: admin

server:
  port: 8001